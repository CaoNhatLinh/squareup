import { useState } from 'react';import { HiX, HiClock, HiMail, HiCheckCircle } from 'react-icons/hi';export default function PendingInvitationsCard({ invitations = [], onCancel, onResend }) {    const [processing, setProcessing] = useState(null);    const handleCancel = async (invitationId, email) => {        if (!window.confirm(`Are you sure you want to cancel the invitation for ${email}?`)) {            return;        }        setProcessing(invitationId);        try {            await onCancel(invitationId);        } finally {            setProcessing(null);        }    };    const handleResend = async (invitationId) => {        setProcessing(invitationId);        try {            await onResend(invitationId);        } finally {            setProcessing(null);        }    };    const getTimeRemaining = (expiresAt) => {        const now = Date.now();        const remaining = expiresAt - now;        if (remaining <= 0) return { text: 'Expired', color: 'text-red-600', expired: true };        const minutes = Math.floor(remaining / 1000 / 60);        const hours = Math.floor(minutes / 60);        if (hours > 0) {            return { text: `${hours}h ${minutes % 60}m`, color: 'text-green-600', expired: false };        }        if (minutes > 10) {            return { text: `${minutes}m`, color: 'text-green-600', expired: false };        }        if (minutes > 5) {            return { text: `${minutes}m`, color: 'text-amber-600', expired: false };        }        return { text: `${minutes}m`, color: 'text-red-600', expired: false };    };    if (invitations.length === 0) {        return null;    }    return (        <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden mb-6">            <div className="bg-gradient-to-r from-amber-500 to-orange-500 px-6 py-4">                <div className="flex items-center gap-3">                    <div className="w-10 h-10 rounded-lg bg-white/20 backdrop-blur-sm flex items-center justify-center">                        <HiClock className="w-6 h-6 text-white" />                    </div>                    <div className="flex-1">                        <h3 className="text-white font-semibold text-lg">Pending Invitations</h3>                        <p className="text-amber-100 text-sm">                            {invitations.length} {invitations.length === 1 ? 'invitation' : 'invitations'} awaiting response                        </p>                    </div>                </div>            </div>            <div className="divide-y divide-gray-100">                {invitations.map((invitation) => {                    const timeRemaining = getTimeRemaining(invitation.expiresAt);                    const isProcessing = processing === invitation.id;                    return (                        <div                            key={invitation.id}                            className="px-6 py-4 hover:bg-gray-50 transition-colors"                        >                            <div className="flex items-start gap-4">                                <div className="w-12 h-12 rounded-full bg-gradient-to-br from-indigo-500 to-purple-600 flex items-center justify-center flex-shrink-0">                                    <HiMail className="w-6 h-6 text-white" />                                </div>                                <div className="flex-1 min-w-0">                                    <div className="flex items-start justify-between gap-4 mb-2">                                        <div className="flex-1 min-w-0">                                            <h4 className="font-semibold text-gray-900 truncate">                                                {invitation.email}                                            </h4>                                            <p className="text-sm text-gray-600">                                                Role: <span className="font-medium text-gray-900">{invitation.roleName}</span>                                            </p>                                        </div>                                        <div className={`flex items-center gap-1.5 px-3 py-1.5 rounded-full ${timeRemaining.expired                                                ? 'bg-red-100 border border-red-200'                                                : 'bg-gray-100 border border-gray-200'                                            }`}>                                            <HiClock className={`w-4 h-4 ${timeRemaining.color}`} />                                            <span className={`text-sm font-medium ${timeRemaining.color}`}>                                                {timeRemaining.text}                                            </span>                                        </div>                                    </div>                                    <div className="flex flex-wrap items-center gap-x-4 gap-y-1 text-xs text-gray-500 mb-3">                                        <span className="flex items-center gap-1">                                            <HiCheckCircle className="w-3.5 h-3.5" />                                            Sent: {new Date(invitation.createdAt).toLocaleDateString('vi-VN', {                                                day: '2-digit',                                                month: '2-digit',                                                year: 'numeric',                                                hour: '2-digit',                                                minute: '2-digit'                                            })}                                        </span>                                    </div>                                    <div className="flex gap-2">                                        <button                                            onClick={() => handleResend(invitation.id)}                                            disabled={isProcessing}                                            className="px-3 py-1.5 text-sm font-medium text-indigo-600 hover:bg-indigo-50 rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed"                                        >                                            {isProcessing ? 'Resending...' : 'Resend'}                                        </button>                                        <button                                            onClick={() => handleCancel(invitation.id, invitation.email)}                                            disabled={isProcessing}                                            className="px-3 py-1.5 text-sm font-medium text-red-600 hover:bg-red-50 rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed flex items-center gap-1.5"                                        >                                            <HiX className="w-4 h-4" />                                            Cancel                                        </button>                                    </div>                                </div>                            </div>                        </div>                    );                })}            </div>        </div>    );}