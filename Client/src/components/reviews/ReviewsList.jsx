import { HiOutlineTag, HiOutlineClock, HiOutlineChatBubbleLeftRight } from "react-icons/hi2";const StarDisplay = ({ rating, size = "w-5 h-5" }) => (  <div className="flex gap-0.5">    {[1, 2, 3, 4, 5].map((star) => (      <svg        key={star}        className={`${size} ${          star <= rating ? "text-yellow-500 fill-current" : "text-gray-300"        }`}        fill="none"        stroke="currentColor"        viewBox="0 0 24 24"      >        <path          strokeLinecap="round"          strokeLinejoin="round"          strokeWidth={2}          d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"        />      </svg>    ))}  </div>);const ReviewsList = ({ reviews }) => {  if (reviews.length === 0) {    return (      <div className="text-center py-12 bg-gray-50 rounded-xl border border-dashed border-gray-300">        <HiOutlineChatBubbleLeftRight className="w-16 h-16 mx-auto text-gray-400 mb-4" />        <p className="text-gray-600">          No reviews match the current filters.        </p>      </div>    );  }  return (    <div className="space-y-6">      {reviews.map((review) => (        <div          key={review.id}          className="border border-gray-200 rounded-xl p-6 bg-white shadow-sm"        >          <div className="flex items-start justify-between mb-4 pb-3 border-b border-gray-100">            <div>              <div className="flex items-center gap-3 mb-2">                <StarDisplay rating={review.rating} size="w-5 h-5" />                <span className="font-extrabold text-lg text-red-600">                  {review.rating}.0                </span>              </div>              <p className="text-xs text-gray-500 flex items-center gap-4">                <span className="flex items-center gap-1">                  <HiOutlineTag className="w-4 h-4" />                  Order #                  <code className="font-mono font-medium text-gray-700">                    {review.orderId?.slice(-6).toUpperCase()}                  </code>                </span>                <span className="flex items-center gap-1">                  <HiOutlineClock className="w-4 h-4" />                  {new Date(review.createdAt).toLocaleDateString(                    "en-US",                    {                      year: "numeric",                      month: "short",                      day: "numeric",                    }                  )}                </span>              </p>            </div>          </div>          {review.feedback && (            <p className="text-gray-700 mb-4 leading-relaxed bg-red-50 p-3 rounded-lg border border-red-100 italic">              "{review.feedback}"            </p>          )}          {review.itemReviews && review.itemReviews.length > 0 && (            <div className="mt-4 pt-4 border-t border-gray-100">              <h4 className="text-sm font-semibold text-gray-900 mb-3">                Item Specific Feedback ({review.itemReviews.length})              </h4>              <div className="space-y-3">                {review.itemReviews.map((item, idx) => (                  <div                    key={idx}                    className="flex items-start gap-3 bg-gray-50 rounded-lg p-3 border border-gray-200"                  >                    {item.image && (                      <img                        src={item.image}                        alt={item.name}                        className="w-12 h-12 object-cover rounded flex-shrink-0"                      />                    )}                    <div className="flex-1 min-w-0">                      <div className="flex items-center gap-2 mb-1">                        <p className="font-medium text-sm text-gray-900">                          {item.name}                        </p>                        <StarDisplay                          rating={item.rating}                          size="w-3 h-3"                        />                      </div>                      {item.feedback && (                        <p className="text-xs text-gray-600 leading-relaxed italic">                          "{item.feedback}"                        </p>                      )}                    </div>                  </div>                ))}              </div>            </div>          )}        </div>      ))}    </div>  );};export default ReviewsList;