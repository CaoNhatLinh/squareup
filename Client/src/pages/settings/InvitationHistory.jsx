import { useRestaurant } from "@/hooks/useRestaurant";import PageHeader from '@/components/common/PageHeader';import InvitationHistoryCard from '@/components/settings/InvitationHistoryCard';import { HiArrowLeft } from 'react-icons/hi';import { useNavigate } from 'react-router-dom';import Button from '@/components/ui/Button';export default function InvitationHistory() {    const { restaurant } = useRestaurant();    const restaurantId = restaurant?.id;    const navigate = useNavigate();    if (!restaurantId) {        return (            <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">                <div className="text-center py-12">                    <p className="text-gray-600">Loading...</p>                </div>            </div>        );    }    return (        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">            <PageHeader                title="Invitation History"                subtitle="View and manage all invitation records"                rightChildren={(                    <Button                        variant="secondary"                        size="medium"                        icon={HiArrowLeft}                        onClick={() => navigate(`/${restaurantId}/settings/staff`)}                    >                        Back to Staff                    </Button>                )}            />            <InvitationHistoryCard restaurantId={restaurantId} />        </div>    );}